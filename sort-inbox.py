# DEPRECATED — delete this file
# Inbox routing is handled by the /content-extract skill.
# Run `/content-extract` in Claude Code from this vault — it reads MODS.md,
# pulls from the Notion inbox, routes content to the correct folder, and archives.
raise SystemExit("Use /content-extract instead.")
